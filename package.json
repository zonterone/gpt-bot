{
  "dependencies": {
    "dotenv": "^16.0.3",
    "openai": "^3.2.1",
    "telegraf": "^4.12.2"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.2",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "build": "rm -rf dist/* && tsc",
    "dev": "yarn build && node dist/bot.js"
  }
}
